'use strict';

export function CreateRoute({
  base_url,
  tabElement,
  map,
  layer調査ルート,
  set調査地点Source,
  create明細行,
  ajaxExecute,
  geojsonFormatter,
  showAlert,
  showConfirm,
  proj3857,
  proj4326
}) {


  return {
  };
}
